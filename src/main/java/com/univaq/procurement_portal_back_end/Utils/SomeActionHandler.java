/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package com.univaq.procurement_portal_back_end.Utils;

import com.univaq.procurement_portal_back_end.data.dao.NotificationDAO;
import com.univaq.procurement_portal_back_end.data.model.NotificationDAOImpl;
import com.univaq.procurement_portal_back_end.resources.NotificationService;
import java.sql.SQLException;

/**
 *
 * @author BATOUL
 */
public class SomeActionHandler {
    /*
    private NotificationService notificationService = new NotificationService();
    private NotificationDAO notificationDAO = new NotificationDAOImpl();

    public void performAction(String userRole, String actionDetails) {
        // Perform the action logic here

        // Notify and save notification
        String message = userRole + " performed an action: " + actionDetails;
        notificationService.notifyAction(message);
        try {
            notificationDAO.saveNotification(userRole, message);
        } catch (SQLException e) {
            e.printStackTrace();
        }
    }*/
}
